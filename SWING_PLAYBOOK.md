# 波段交易手册 (Swing Playbook)

把「四步波段流程」从**聊天提示词**变成**仓库里的代码**。提示词每次都要重新讲一遍、
每次结果都不一样;代码每天跑出来的是同一套规则、同一份可回溯的报告。

流程本身来自一个常见的 4 步法(system prompt → 选股 → 图表分析 → 交易方案),
这里把它落成两个脚本:

| 步骤 | 原来靠人/提示词 | 现在的实现 |
|------|----------------|-----------|
| 1 账户与纪律 | 写在 system prompt 里 | `swing_analysis.py` 顶部的 `ACCOUNT_USD` / `RISK_PCT` 等常量(可用环境变量覆盖) |
| 2 选股 (screening) | Xynth / TradingView 手动筛 | `screen()` — ATR 波动带 + 成交额 + 早期趋势强度 |
| 3 图表技术分析 | 把 5 张图喂给模型 | `features()` + `score()` + `patterns()` — 100 分制评分卡,选出最强的一只 |
| 4 生成交易方案 | 让模型写 3 个 setup | `build_setups()` — 3 个**技术逻辑互不相同**的方案,含仓位与盈亏金额 |

数据由 `swing_history.py` 在 GitHub Actions runner 上抓(Yahoo 日线,1 年),
提交进仓库;分析脚本只读已提交的 `swing_history.json`。
**原因:Claude sandbox 的出口代理封了 Yahoo(403),行情必须在 runner 上取。**
这和 `quotes.py` 的做法完全一致。

```bash
python swing_analysis.py --selftest   # 指标数学自检(无需行情,本地可跑)
python swing_history.py               # 只在 Actions runner 上有用
python swing_analysis.py              # 出 reports/swing/YYYY-MM-DD.md + swing_setups.json
```

---

## Step 1 — 交易授权 (mandate)

- 账户 **$1,000**,每笔风险 **2%(=$20)**,不加杠杆(仓位上限 = 账户的 100%)。
- 持仓周期:**几天到几周**。只做多、只做正股 —— 不做期权、不做复杂结构。
- 只做多是刻意的:筛选逻辑找的是「趋势刚起来」的票,而 $1,000 的现金账户做空
  既不方便、风险也不对称。

> 为什么固定 2%:$20 的风险预算配上 4-5% ATR 的票,自然只买得起 1-3 股。
> 这不是 bug,是仓位管理在替你拒绝「一把梭」。想拿更大仓位就得先有更大账户,
> 而不是把止损放宽。

## Step 2 — 选股

硬性闸门,任何一条不过就出局:

| 条件 | 阈值 | 为什么 |
|------|------|--------|
| 中等波动 | 4% < ATR(14)/价格 < 5% | 太闷(<4%)几周也走不到目标;太野(>5%)止损宽到 $1,000 买不了 1 股 |
| 流动性 | 20 日日均成交额 ≥ $50M | 滑点会吃掉小账户的全部利润 |
| 股价 | ≥ $5 | 低价股的点差和仙股风险 |
| 趋势结构 | 价格 > SMA20 > SMA50,且 SMA20 上行 | 顺势,不接下跌中的刀 |
| 趋势强度 | ADX(14) ≥ 20 **且比 5 天前更高** | ADX 在升 = 趋势「刚起来」;ADX 在降 = 趋势走完了 |
| 动量未透支 | 50 ≤ RSI(14) ≤ 72 | 低于 50 没启动,高于 72 是追高 |
| 不追高 | 距 SMA20 ≤ 2.5 × ATR | 仓位的每一分风险都该买在离支撑近的地方 |
| 相对强度 | 20 日跑赢 SPY | 强于大盘的票,回调时更容易被接住 |

按 100 分制排序取前 5。**如果严格档不足 5 只,才用放宽的 ATR 带(±1%)补位,
并在报告里用 `*` 明确标记为二线**——宁可少给几只,也不假装筛出来了 5 只。

## Step 3 — 技术评分卡(选最强的那张图)

| 维度 | 分值 | 看什么 |
|------|------|--------|
| 趋势结构 | 25 | 价格/20/50/200 均线的排列 |
| 动量 | 25 | ADX 水平与方向、MACD 柱正且放大、RSI 甜区 55-68 |
| 量能 | 20 | 5 日量 / 20 日量;上涨日成交量 vs 下跌日成交量(吸筹 or 派发) |
| 相对强度 | 20 | 20 日、60 日相对 SPY 的超额收益 |
| 入场质量 | 10 | 离 SMA20 越近越好 —— 止损窄、空间大 |

同时输出人话版的形态判断:高点抬高/低点抬高、10 日区间只有 N×ATR(缩量盘整/旗形)、
距 60 日高点 2% 以内(突破区)、吸筹/派发等。

## Step 4 — 三个方案(必须是三种不同的技术逻辑)

| 方案 | 入场 | 止损 | 目标 | 性格 |
|------|------|------|------|------|
| **A 突破续势** | 20 日高点上方 0.2%(**stop 买单**) | 10 日底部下方,最宽 2×ATR | 量度涨幅(区间高度)或 2R,取大者 | 主动型:不破不做 |
| **B 20 日均线回踩** | 上升的 SMA20(**limit 买单**) | 下方 1.5×ATR(必要时压到 SMA50 下,最宽 2.5×ATR) | 前高,或 2R,取大者 | 顺势买回调:不追高 |
| **C 斐波那契 38.2% 回撤** | 60 日那段主升浪的 38.2% 位 | 61.8% 位下方 0.5×ATR | 回踩该段高点 | 耐心型:止损最宽,所以仓位最小 |

每个方案都算出:每股风险、股数(风险预算 ÷ 每股风险,再被现金上限截断)、
**止损时亏多少美元、到目标赚多少美元、盈亏比、预计持有天数**
(按每天推进约 0.55×ATR 估算)。

自动打上的警告:
- 盈亏比 < 1.5 → 「边际太薄,可以不做」;
- 股数 = 0 → 「一股的风险已经超过 $20 预算,需要碎股或更大账户」;
- 仓位被现金截断 → 「实际风险低于 2% 预算」。

**三个方案是同一只票的三种买法,不是三个仓位。** 同时上三个 = 单票风险 ×3。

## 大盘环境 (regime)

报告顶部会给出 SPY 的状态,直接决定要不要动手:

- **risk-on**(SPY 在 50 与 200 日线之上)→ 正常仓位;
- **mixed**(跌破 50 日但守住 200 日)→ 半仓,只做评分最高的那一个;
- **risk-off**(跌破 200 日,或 5 日跌超 3%)→ 空仓观望,方案只做模拟。

这条不是凑数的:`learnings.md` 里最贵的一课就是「在半导体熊市里买了漂亮的半导体图」。

## 纪律(三个方案通用)

1. **+1R 先减半仓,止损移到成本价,剩下的沿 SMA20 移动止盈。**
   仓位管理里最贵的一课是「锁利 > 死扛」(Week 1 的 OXY 就是这么拿到 +3% 的)。
2. 日线收盘跌破 SMA20 且放量 → 提前离场,不等止损。
3. 驱动逻辑(催化剂)反转 → 立刻走,哪怕还没到止损。
4. 止损以**日线收盘**为准;4-5% ATR 的票,盘中插针是噪音。

## 诚实的部分

- 这是**算法生成的研究**,不是人工分析,更不是投资建议。指标是滞后的,评分卡是
  我定的权重,回撤位是几何画法——没有任何一条保证盈利。
- 本仓库自己的记录:股票账本 3 周 +$20.53,其中 +$18.34 来自第 1 周的能源行情,
  第 2、3 周基本打平。**结论是「没有可重复的 edge」,不是「策略有效」。**
  详见 `learnings.md`。
- 这套流程还**没有**经过前向验证。上真金白银之前的门槛不变:
  4 周 / 20 笔以上 / 净盈利 / 零数据错误 —— 先在模拟盘跑够。
- `swing_analysis.py` 不下单、不碰券商,只写报告。要交易,人自己下单。
