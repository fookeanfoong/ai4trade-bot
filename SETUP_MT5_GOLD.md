# 黄金 EA 挂 MT5 自动跑 — 操作手册

对应文件：**`mql5/GoldScalper.mq5`**（XAUUSD / MQL5 / MetaTrader 5）

这份文档只讲"怎么把它挂上去自动跑"。策略逻辑和参数为什么是这些值，
看 [`reports/gold_research.md`](reports/gold_research.md)（样本外验证）和
[`reports/gold_backtest.md`](reports/gold_backtest.md)（回测）。

> ⚠️ **先看这一条**：验证结果是 M15 · 固定$3止损 · RR1:2，训练 +0.138R / 验证 +0.056R。
> 这是"不是纯噪音"，**不是"会赚钱"**。真实点差、滑点、隔夜利息都没算进去，
> 只会让结果更差。**先在模拟账户跑满 30 笔再谈真金白银。**

---

## 0. 先确认三件事（5 分钟，能省掉后面 80% 的坑）

打开 MT5 → **市场报价**（Market Watch）→ 找到黄金品种 → 右键 → **规格**（Specification）：

| 要看的项 | 你的值 | 为什么重要 |
|---|---|---|
| 品种名 | `XAUUSD` / `XAUUSD.sml` / `XAUUSDm`… | 后缀不影响，EA 用 `_Symbol` 自动读当前图表品种 |
| 合约大小 | 通常 100（盎司） | 100oz 时 0.01 手 = 1oz = 金价每动 $1 盈亏 $1 |
| 最小手数 / 手数步长 | 0.01 / 0.01（`.sml` 可能是 0.001） | EA 已按步长归一，不会把 0.006 抹成 0.01 |
| 点差（Spread 列） | 平时 $0.20~$0.40 | 常态点差 >$0.50 的话 EA 会**整天不出手**（见 §6） |

**本金门槛（硬算，不是建议）**：默认单笔风险硬上限 2%，固定止损 $3。
最小手 0.01 手下这一单的风险就是 $3 → 需要净值 ≥ `$3 / 2% = $150`。
**净值低于 $150，EA 会拒绝开单**，日志打印「拒绝开仓：最小可下手数…超过上限」。
这是设计，不是 bug —— 钱不够就不该带着超标风险开。

---

## 1. 装 EA

1. 下载 `mql5/GoldScalper.mq5`
   （GitHub 页面上打开该文件 → 右上 **Download raw file**）。
2. MT5 里：**文件 → 打开数据文件夹**（File → Open Data Folder）。
3. 把 `GoldScalper.mq5` 放进 `MQL5\Experts\` 目录。
4. MT5 里：**工具 → MetaQuotes Language Editor**（或按 `F4`）→ 左侧
   Navigator 找到 `GoldScalper.mq5` 双击打开 → 按 **`F7` 编译**。
   - 底部出现 `0 errors, 0 warnings` = 成功，同目录生成 `GoldScalper.ex5`。
   - 报错先别改代码，把报错原文发我。
5. 回到 MT5 → 导航器（`Ctrl+N`）→ **智能交易系统** → 右键 → **刷新**，
   列表里应出现 `GoldScalper`。

---

## 2. 先跑策略测试器（别跳过这步）

`Ctrl+R` 打开策略测试：

- 智能交易系统：`GoldScalper`
- 品种：你的黄金品种　| 周期：**M15**
- 模型：**每笔订单**（Every tick based on real ticks）
- 日期：最近 3 个月　| 存款：$200　| 杠杆：按你的实际账户

跑完看两个数字：**交易笔数**和**净利**。3 个月 M15 大概应有 30~80 笔。
**笔数为 0** = 参数或品种不对，去 §6 排查，不要直接挂实盘。

> 测试器结果一定比实盘好看：它用的是历史点差，不含真实滑点。

---

## 3. 挂上图表

1. 打开**黄金 M15 图表**（品种双击 → 图表周期选 M15）。
2. 导航器里把 `GoldScalper` **拖到这张图上**。
3. 弹窗 → **常用**标签页 → 勾选 **允许算法交易**（Allow Algo Trading）。
4. **输入参数**标签页 → 按 §4 核对参数 → **确定**。
5. 点工具栏 **算法交易**（Algo Trading）按钮，或按 `Ctrl+E`，让它变绿。

**确认真的跑起来了 —— 只认这一条证据**：
打开**工具箱 → 智能交易系统**（Toolbox → Experts）标签，应看到：

```
GoldScalper 启动 | XAUUSD PERIOD_M15 | 净值 200.00 | 合约 100 | 最小手 0.010 | ...
自检:最小手 0.010 × 止损 3.00 → 风险 $3.00 = 净值的 1.500%
```

这两行出现 = EA 活着并且已经算过你的账户。图表右上角有 EA 名字但日志里没有这两行，
说明算法交易没开。

---

## 4. 参数：只有一个必须改

其余默认值都来自验证结果，**别动**。要动的是这个：

### ⚠️ 时段过滤用的是**服务器时间**，不是你的本地时间

`InpSessionStartHour=7` / `InpSessionEndHour=20` 是按 **GMT+0** 服务器写的。
多数券商服务器是 GMT+2/+3，照默认值跑等于把交易时段整体提前 2~3 小时，
会错过纽约盘、多做亚洲盘（点差最烂的时段）。

**查你的服务器时间**：市场报价窗口顶部显示的时间，就是服务器时间。
拿它和 UTC 比，得出偏移，然后按下表改：

| 服务器时区 | InpSessionStartHour | InpSessionEndHour |
|---|---|---|
| GMT+0 | 7 | 20 |
| GMT+2 | 9 | 22 |
| GMT+3（夏令时常见） | 10 | 23 |

（覆盖伦敦开盘 → 纽约午后收，夏令时切换时会差 1 小时，不必追。）

### 其余关键参数（默认值即可）

| 参数 | 默认 | 说明 |
|---|---|---|
| `InpTimeframe` | M15 | **必须和图表周期一致**。H1 在验证里十组配置全为负，别换 |
| `InpFixedStopUSD` | 3.0 | 固定 $3 止损（金价距离），验证过的那组 |
| `InpRewardRisk` | 2.0 | RR 1:2。改成 1.5 会让期望值转负 |
| `InpRiskPercent` | 1.0 | 单笔风险 1%；`InpMaxRiskPercent=2.0` 是夹不穿的硬顶 |
| `InpDailyLossPct` | 4.0 | 当日亏 4% 自动停手到次日 |
| `InpMaxPositions` | 1 | 同时只持 1 单 |
| `InpMaxSpreadUSD` | 0.50 | 点差超 $0.50 不交易。**不出手时不要调大这个数**，见 §6 |
| `InpMagic` | 20260809 | 单号标记。同账户跑第二个 EA 时必须改成别的数 |

---

## 5. 让它 24 小时跑 —— 关电脑就停了

MT5 的 EA 跑在**终端里**。关掉 MT5、笔记本合盖、断网、系统重启 → EA 停。
**手机版 MT5 不能跑 EA**（只能看盘和手动下单）。所以要么电脑常开，要么用 VPS：

**最省事：MetaQuotes VPS**（MT5 内置）
图表上右键 → **虚拟主机** → 注册虚拟服务器 → 选延迟最低的机房 → 付费（约 $10~15/月）→ **迁移**（Migrate）。

迁移后本地 MT5 可以关掉，EA 在 VPS 上继续跑。

> **最容易踩的坑**：以后每次改参数，改的只是本地那份。
> **必须再点一次「迁移」**，VPS 上跑的才会变。不迁移 = 你以为改了，其实没改。

---

## 6. 不出单？按这个顺序查（从最常见排起）

| 现象 | 原因 | 怎么确认 / 处理 |
|---|---|---|
| 日志里连启动那两行都没有 | 算法交易没开 | `Ctrl+E` 让按钮变绿；EA 弹窗里也要勾"允许算法交易" |
| 日志刷「点差 X 超过上限」 | 券商点差太宽 | 看市场报价的 Spread 列。常态 >$0.50 的话是**券商/账户类型问题**，换 ECN 类账户；调大 `InpMaxSpreadUSD` 只是把成本吞回自己身上 |
| 日志安静，什么都不打印 | 时段过滤挡住了 | §4 的服务器时间没改对；或现在是周末 / 周五 18:00 后 |
| 有信号但从不开单 | 均线缠绕 = 无趋势 | 正常。EA 要求 快EMA>慢EMA>200EMA 才做多（反之做空），震荡市本来就该不做 |
| 日志打「拒绝开仓…超过上限」 | 本金不够 | 净值需 ≥$150（见 §0）。加钱，或换合约更小的品种 |
| 「当日亏损…达到上限」 | 熔断触发了 | 今天不再开新仓，次日 0 点自动重置。别手动关了重开绕过它 |
| 开单失败 retcode=… | 券商拒单 | 把 retcode 和描述发我，多半是填充模式或最小止损距离 |

---

## 7. 停手 / 急停

- **停止开新仓**（保留现有持仓和它的止损）：按 `Ctrl+E` 关掉算法交易。
- **彻底移除**：图表右键 → 智能交易系统 → 删除。
- **VPS 上的**：图表右键 → 虚拟主机 → 停止/删除虚拟服务器。**光关本地 MT5 没用，VPS 还在跑。**

> 每一单的止损都是**下单时一起提交给券商**的。EA 停了、终端关了、VPS 挂了，
> 止损依然在券商服务器上活着。这是这个 EA 设计上最重要的一条，不要用手动改单破坏它。

---

## 8. 上实盘前的检查清单

- [ ] 策略测试器 3 个月跑出 ≥30 笔
- [ ] 模拟账户挂满 **30 笔真实成交**
- [ ] 服务器时间偏移已按 §4 改对
- [ ] 第一单成交后，去**交易**标签确认这单的 **S/L 和 T/P 两列都有数字**（不是空的）
- [ ] 净值 ≥$150，且这笔钱**全亏光也不影响生活**

模拟 30 笔之后，把 MT5 的**账户历史**导出（历史标签右键 → 报告 → HTML），
发我，我按真实成交而不是回测数据重新算期望值。
