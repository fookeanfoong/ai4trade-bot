# 黄金 EA 挂 MT5 自动跑 — 部署手册

主 EA：**`mql5/XAUUSD_ScalperGuard.mq5`**（XAUUSD M5 / 风控优先）

- 策略规格 → 代码的逐条对照，看 [`mql5/README_ScalperGuard.md`](mql5/README_ScalperGuard.md)
- 这份文档只管一件事：**怎么把它挂上去、让它自己跑、以及它不出单时怎么查**

> `mql5/GoldScalper.mq5` 是更早的一版（M15，只有 6 项过滤）。两个别同时挂在同一个账户上 ——
> 它们 Magic Number 不同不会互相平仓，但会各自按各自的风控开仓，合计风险不受任何一方控制。

---

## 一、先算你的账户跑不跑得动（最重要的一步）

ScalperGuard 有两道会直接导致「永远 NO TRADE」的硬闸门。挂之前先用下面的表对一下，
省得挂上去等一星期发现一笔都没开。

**基准**：金价 ≈ $4,400/盎司，标准合约 100 盎司/手 → 0.01 手 = 1 盎司，金价每动 $1 盈亏 $1。

### 闸门 1：保证金（`InpMaxMarginPctPerPos = 35%`，对照**可用保证金**）

| 黄金杠杆 | 0.01 手保证金 | 需要的可用保证金（÷35%） | $200 账户 |
|---|---|---|---|
| 1:20 | $220 | **$629** | ❌ 开不了 |
| 1:50 | $88 | $251 | ❌ 差一点 |
| 1:100 | $44 | $126 | ✅ |
| 1:200 | $22 | $63 | ✅ |
| 1:500 | $8.80 | $25 | ✅ |

**很多经纪商对黄金单独设杠杆**，不等于你的账户杠杆。EA 启动时会把实际数字打进日志。

### 闸门 2：风险（`CalcLot()` — 最小手的风险超过预算就拒单，绝不缩止损凑数）

0.01 手时"止损多少美元 = 亏多少美元"，所以 **能承受的止损 = 余额 × 风险%**。

黄金 **M5 的 ATR 现在约 $4.14**（见 `reports/gold_research.md`），止损 = swing 高低点之外
再加 `0.6 × ATR` ≈ $2.5 —— 光这个缓冲就超过 $200 账户 1% 的 $2 额度。
结构止损实际落在 **$3~$6**。

`InpSmallAccountEscalate`（默认开）会在最小手开不了时，把这一笔的风险
**上调到规格自己的 2% 上限**，而不是缩止损。$200 + 0.01 最小手的效果：

| 结构止损 | 1% 预算 $2 | 救济后 | 结果 |
|---|---|---|---|
| $2.00 | 够 | 不触发 | ✅ 1.00%，风险 $2 |
| $3.00 | 不够 | 上调至 1.50% | ✅ 0.01 手，风险 $3 |
| $3.99 | 不够 | 上调至 2.00% | ✅ 风险 $3.99 |
| **$4.00** | 不够 | 上调至 2.00%（临界点） | ✅ 风险 $4.00 |
| $4.01 | 不够 | 抬到上限也不够 | ❌ 拒单 |
| $6.00 | 不够 | 抬到上限也不够 | ❌ 拒单 |

**$200 + 0.01 最小手 = 只接得住 $4.00 以内的止损**，再宽的照样拒。
按 M5 ATR $4.14 算，大约能接住一半多一点的信号 —— 从"几乎全拒"到"过一半"。

**代价必须讲清楚**：单笔最大亏损从 $2 变 $4，日内 -$15 熔断从能扛 7.5 笔变成 3.75 笔。
但连亏 3 笔本来就停手（-$12），所以先触发的仍然是连亏闸门 —— 这个改动没有突破
你规格里的任何一条上限，只是用掉了 1%~2% 区间的上半段。

救济在这三种情况下**不生效**（降险优先级更高）：连亏 ≥2 的观察模式、
当日盈利 ≥$20 的保守模式、当日剩余亏损额度不足。

### 更好的路：换颗粒度，而不是加风险

**最小手数 0.001 的微型黄金**（`XAUUSD.sml` / `XAUUSDm` / `MGC` 之类），0.001 手 = 0.1 盎司：

| 结构止损 | $200 账户 · 0.001 最小手 |
|---|---|
| $3.00 | ✅ 0.006 手，风险 $1.80 |
| $4.00 | ✅ 0.005 手，风险 $2.00 |
| $6.00 | ✅ 0.003 手，风险 $1.80 |

**整个止损区间全接得住，而且风险始终停在 1%，一次救济都不用触发。**
这才是干净的解法 —— 救济是妥协，换品种不是。

EA 启动时会自动扫经纪商的品种表，存在颗粒度更细的黄金就直接点名：

```
[HINT] 发现颗粒度更细的品种：XAUUSD.sml —— 每 $1 止损距离，最小手风险 $0.10，
       而当前 XAUUSD 是 $1.00（细 10 倍）。小账户把 EA 挂到 XAUUSD.sml 的 M5 图表上……
```

第三条路：余额加到 **$500 左右**（0.01 最小手下 1% = $5，覆盖整个止损区间，救济也不用触发）。

> **不要**去调大 `InpRiskPctDefault` / `InpRiskPctMax` —— 上限 2% 是 `OnInit` 里写死校验的，
> 填超过直接拒绝启动。救济已经把 1%~2% 这段用尽，再往上就是改规格，不是调参数。

### 一行日志代替以上全部计算

挂上去之后，看「智能交易系统」日志里 `[INIT]` 那一行结尾：

```
1%风险($2.00)对应最大止损 $2.00/盎司；2%风险($4.00)对应 $4.00/盎司
```

**这个数 < $3 → 你的账户跑不动这套风控**，先解决账户，别急着看信号。

---

## 二、装上去

1. MT5 → `文件` → **打开数据文件夹**
2. 把 `mql5/XAUUSD_ScalperGuard.mq5` 复制进 `MQL5\Experts\`
3. MT5 里按 `F4` 开 MetaEditor → 双击该文件 → **`F7` 编译** → 应显示 `0 errors`
4. 回 MT5 → 导航器（`Ctrl+N`）→ 智能交易系统 → 右键**刷新**
5. 打开 **XAUUSD 的 M5 图表**
6. 把 `XAUUSD_ScalperGuard` 拖到图表上 → 弹窗勾选 **允许算法交易** → 确定
7. 工具栏 **算法交易** 按钮点亮（或 `Ctrl+E`）

> 我这边没有 MetaEditor（Linux 环境编译不了 MQL5），**编译这步的结果以你机器上为准**。
> 报错就把 MetaEditor 底部的原文发我。

**确认它真的活着 —— 两个证据：**
- 图表左上角出现状态面板（账户 DEMO/REAL、当日盈亏、笔数、连亏、模式、点差、ATR、状态）
- 日志里有 `[INIT]` 那一行

面板不出现 = EA 没跑起来（多半是算法交易没开，或 `OnInit` 被闸门拒了，看日志）。

---

## 三、唯一必须改的参数：时段是**服务器时间**

`InpSessionStartHour=8` / `InpSessionEndHour=21` 用的是 `TimeCurrent()`，即**经纪商服务器时间**，
不是你的本地时间。多数经纪商服务器在 GMT+2/+3，照默认值跑会把交易窗口整体前移 2~3 小时，
变成多做亚洲盘（点差最烂、`InpSpreadVsATRMax` 最容易挡）、少做纽约盘。

**查服务器时间**：市场报价窗口顶部显示的就是。和 UTC 比一下得出偏移，然后：

| 服务器时区 | Start | End | FlatAllBefore |
|---|---|---|---|
| GMT+0 | 8 | 21 | 22 |
| GMT+2 | 10 | 23 | 24（改 0 无效，用 23） |
| GMT+3（夏令时常见） | 11 | 23 | 23 |

（覆盖伦敦开盘 → 纽约午后。夏令时切换会差 1 小时，不必追。）

⚠️ GMT+2/+3 下 `InpFlatAllBeforeHour` 顶到 23~24 会和 `InpSessionEndHour` 挤在一起。
`SessionOk()` 里周五用的是 `FlatAllBefore - 2`，所以这两个值至少留 1 小时间隔。

其余参数按 `mql5/README_ScalperGuard.md` 第四节，默认即可。

---

## 四、让它 24 小时跑 —— 关电脑就停

EA 跑在**终端进程里**。关 MT5、笔记本合盖、断网、重启 → EA 停止工作。
**手机版 MT5 不能跑 EA。** 要连续跑就得有台常开的机器：

**最省事：MetaQuotes VPS（MT5 内置）**
图表上右键 → **虚拟主机** → 注册虚拟服务器 → 选延迟最低的机房 → 付费（约 $10~15/月）→ **迁移**。

迁移完本地 MT5 可以关，EA 在 VPS 上继续跑。

> **两个必踩的坑：**
> 1. **改完参数必须再点一次「迁移」**，否则改的只是本地那份，VPS 上跑的还是旧的。
> 2. 迁移到 VPS 后 `Files\XAUUSD_ScalperGuard_log.csv` 写在 **VPS 上**，本地看不到。
>    要看决策日志得用 MT5 的「日志」标签（走的是终端日志，会同步回来），或先停掉迁移。

**已有持仓的止损不受影响** —— 每笔单的 SL 是下单时就提交给经纪商的，
EA 停了、VPS 挂了，止损依然在经纪商服务器上生效。

---

## 五、它不出单？按日志里的原因对号入座

面板「状态」那行和日志里的 `[NO-TRADE]` 会直接告诉你原因。下面按实际出现频率排：

| 日志原因 | 含义 | 该怎么办 |
|---|---|---|
| `最小手数 X 的风险为 $Y…超过允许的 $Z` | **闸门 2**，且抬到 2% 上限仍不够 | 止损宽过 `余额×2%`。回第一节，换 0.001 最小手或加资金。**这是 $200 账户最常见的一条** |
| `最小手 X 在 $Y 止损下需 Z% 风险…自 1.00% 上调` | 小账户救济生效了 | 不是错误，是这一笔按 2% 以内开的。想关掉设 `InpSmallAccountEscalate=false` |
| `保证金 $X 超过上限…杠杆/最小手数不允许` | **闸门 1** | 黄金杠杆太低。找经纪商确认黄金单独杠杆 |
| `趋势不一致 HTF=x LTF=y` | H1 和 M5 方向不一致 | 正常，这是设计。要求两个周期同向 |
| `ADX x < 18，横盘` | 无趋势 | 正常，横盘不做 |
| `横盘：30 根 K 线区间仅 X` / `高频假突破` | 市场质量闸门 | 正常 |
| `点差过大 X > 0.35` | 点差超限 | 看市场报价 Spread 列。常态 >$0.35 是**经纪商问题**，换 ECN 账户；调大这个数只是把成本吞回自己身上 |
| `点差/ATR 异常` | 静市里点差占比过高 | 正常，低波动时段自动停手 |
| `非交易时段（服务器 HH:MM）` | 时段过滤 | 第三节的服务器时间没改对 |
| `结构止损过宽 X > 6.00 USD` | 止损超过 `InpSlMaxUSD` | 正常，宁可不做也不缩止损 |
| `风险回报不足 RR=x < 1.5` | 前方关键位太近 | 正常 |
| `重大数据窗口：…` | 经济日历过滤 | 正常，数据前 30 分钟 / 后 20 分钟不开仓 |
| `连亏 N 笔 — 当天停止` / `已达当日笔数上限` | 熔断 | 次日自动重置。**别手动删了 EA 重挂来绕过** —— 日内统计是从成交历史推导的，重挂也绕不过去（这是故意的） |

**面板都不出现**（不是 NO-TRADE，是完全没反应）：
- `拒绝启动：真实账户 + InpAllowLiveAccount=false` → 你连的是实盘账户。默认只跑 Demo
- `账户不允许 EA 自动交易` → 经纪商侧禁用了 EA
- 什么都没有 → 算法交易按钮没点亮

---

## 六、$200 + 0.01 最小手时，有一个行为会和文档描述不一样

`InpPartialClosePct = 50%` 的 TP1 部分止盈**做不了** —— 0.01 手的一半是 0.005，
低于最小手数，拆不开。代码会走这条分支（第 1024 行）：

```
[TP1] #xxx 达到 1.00R，但手数 0.01 无法拆分，改为保本+追踪
```

也就是说小账户下实际执行的是「到 1R 移保本 + ATR 追踪」，没有落袋那 50%。
这不是 bug，但意味着**你的实际盈利分布会和"TP1 锁一半"的预期不同** ——
更多单子会以保本或追踪止损收场。换 0.001 最小手就正常了。

---

## 七、上实盘前

`InpAllowLiveAccount` **保持 false**。要改之前，先攒够这些数据：

- [ ] Demo 连续跑满 **1 个月 / 30 笔以上**成交
- [ ] 日志里 `[HALT]` 记录：靠 **+$50 停**的多，还是靠 **-$15 停**的多
- [ ] 最大回撤、最长连亏几笔
- [ ] `[NO-TRADE]` 里「最小手数风险超限」出现了多少次 —— 这个数高 = 账户规模和风控不匹配，
      不是策略不行
- [ ] 这笔钱全亏光不影响生活

跑满一个月后，把 `MQL5\Files\XAUUSD_ScalperGuard_log.csv` 和 MT5 账户历史
（历史标签右键 → 报告 → HTML）发我，我按真实成交而不是回测数据算一次期望值和回撤分布。

---

*自动交易可以亏光本金，杠杆产品尤其。这是执行工具，不是投资建议。*
