<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- HMS Push Kit: custom messaging service that bridges Push Kit
             messages/tokens onto the Capacitor PushNotifications plugin.
             COMPLIANCE: notification bodies are forced to the "[Educational]"
             prefix in HmsMessagingService before being displayed. -->
        <service
            android:name=".HmsMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.huawei.push.action.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- HMS Push Kit auto-init toggle. -->
        <meta-data
            android:name="push_kit_auto_init_enabled"
            android:value="true" />
    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Android 13+ runtime notification permission (Push Kit). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- HMS Push Kit. -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- HMS In-App Purchases: allow querying the installed AppGallery / HMS
         Core for IAP capability (Android 11+ package visibility). -->
    <queries>
        <intent>
            <action android:name="com.huawei.hms.core.aidlservice" />
        </intent>
        <package android:name="com.huawei.appmarket" />
    </queries>

</manifest>
