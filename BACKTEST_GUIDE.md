# XAUUSD ScalperGuard —— 回测手册

写这份东西的直接原因：**这套 EA 到今天为止，一次回测都没跑过。**

我们手上全部的证据是 31 笔实盘单：

- 100% 是 BUY —— 做空那套逻辑**一笔都没执行过**，等于完全未经检验的代码
- 全部发生在 8/12–8/21 的一段上涨行情里 —— 一种行情，一个样本
- 胜率 90.3%，但剔除那笔 +7.86R 之后，盈亏比只到 **盈 0.83R / 亏 1.01R**，
  对应的盈亏平衡胜率是 **55%**。也就是说：这套东西现在赚钱靠的是胜率，
  不是赔率；而 90% 的胜率是"单边上涨 + 只做多"造出来的，行情一换就没有了
- 路径 C（扫损反手）实盘触发 **0 次**，路径 D（宽松入场）**1 次**，
  BOS/CHoCH 打分、行情质量闸门 —— 全部是写了但没验证过的机器

在这 31 笔上继续调参数，本质是在一个样本上猜。策略测试器能在几分钟里给出
**几千笔、覆盖上涨/震荡/下跌**的成交，包括那套零样本的做空逻辑。
这是目前投入产出比最高的一件事，比再加第五种入场形态高得多。

---

## 〇、先把 EA 改成"能被回测"的

回测跑不起来 / 跑得极慢，通常不是策略问题，是 EA 里那些**为实盘写的设施**
在回测里变成了灾难。这些已经在代码里修好了，列出来是让你知道改了什么：

| 问题 | 后果 | 修法 |
|---|---|---|
| `ReportTradeStats()` 每 30 秒 `HistorySelect(0, …)` 拉**全部**历史 | 回测里成交数长到几千笔，而"每 30 秒"是**模拟时间** —— 2 年 = 200 万次全历史遍历。优化跑批从几分钟变几小时 | `g_quiet` 时直接 return |
| 每笔 `LogLine()` 写 CSV、每 30 秒写 `status.json` | 纯磁盘开销，回测里没人读 | `g_quiet` 时不写文件 |
| `Print()` / `Comment()` | 优化跑批时是纯浪费 | `g_optim` 时跳过 |
| Demo 闸门 | 某些券商的测试账户 `ACCOUNT_TRADE_MODE` 不报 DEMO → **永远进不了回测** | `isDemo` 加 `|| MQL_TESTER` |
| `SG_R_<ticket>` / `SG_P_<ticket>` 全局变量 | 全局变量是**终端级**的，不随回测结束消失；而每次回测订单号都从 1 重开 —— 上一轮的 `SG_R_1` 会被这一轮的 1 号单读到，**R 倍数算错**，保本/追踪/部分止盈全部踩偏。而且只在回测里发生，实盘查不出来 | 回测启动时 `PurgeFlags()` 清空 |

对应新增一个输入项 `InpTesterQuiet`（默认 true）。想在**单次**回测里留下 CSV
供事后用 `analyze_scalperguard.py` 分析，把它设成 false —— 但优化跑批时务必留 true。

---

## 一、跑一次回测：具体点哪里

MT5 → **查看 → 策略测试器**（Ctrl+R）。

### 设置页

| 栏位 | 填什么 | 为什么 |
|---|---|---|
| 智能交易系统 | `XAUUSD_ScalperGuard` | |
| 交易品种 | `XAUUSD.sml` | **必须**是你实盘那个品种。手数步长、点差、合约大小都不一样 |
| 周期 | `M5` | EA 的 `InpLTF`。周期填错不会报错，只是 `IsNewBar()` 的节奏变了 |
| 日期 | 自定义，**至少 1 年** | 少于 1 年基本盖不住一轮上涨+震荡+下跌 |
| 模型 | 见下 | |
| 存款 | **200**，杠杆按你实盘的 | 配 `ScalperGuard_backtest.set`（虚拟本金已置 0） |
| 延迟 | 先选"随机延迟" | 默认"零延迟"是理想成交，黄金上不现实 |

**输入参数页 → 载入 → `ScalperGuard_backtest.set`。**

### 模型选哪个

- **`每笔即时价格基于真实价格变化`（Every tick based on real ticks）** ——
  用券商服务器上真实的历史 tick，**包含当时真实的买卖价差**。这是零售平台上
  最接近真实的模拟，最终验证必须用它。
- 其它模式（`1 分钟 OHLC`、`仅开盘价`）快得多，**只用来做参数初筛**。
  我们这套 EA 在 K 线内做追踪止损、保本、部分止盈、时间离场 —— 这些全都
  依赖 K 线**内部**的价格路径，用 OHLC 模式测出来的结果会系统性偏乐观。

跑完看报告顶部的 **History Quality / 建模质量**。注意一个容易被忽略的点：
显示 99% 的意思是"这段区间里有 99% 能用真实 tick 重放"，缺 tick 的那部分
MT5 会**自己生成 tick 顶上**。所以先在 **工具 → 选项 → 图表 → 下载**
或直接在测试器里让它把 tick 数据下全，再跑正式的那一遍。

---

## 二、第一件该做的事：不是调参，是**看做空**

第一次跑完，别急着看净利。打开 **图表** 标签页和 **交易** 标签页，回答三个问题：

1. **有没有 SELL？占比多少？**
   如果做空单占比远低于 40%，说明多空不对称 —— 要么是某个过滤器
   （趋势判定、扫损方向、关键位取法）对空头更严，要么是空头分支有 bug。
   这条比任何参数都值钱：**它是唯一能暴露"零样本代码"的办法。**
2. **SELL 的胜率/R 分布和 BUY 差多少？**
   差得多 = 这套逻辑本质是个多头策略，实盘一旦转熊会直接失血。
3. **路径 C（扫损）和路径 D（宽松）各触发了多少次？**
   如果还是接近 0，说明这两条路在参数下是死代码，那就该删掉或改条件，
   而不是留在那里制造"我有四种入场"的错觉。

把回测按年份/季度切开单独跑，找出**最差的那一段**（震荡或下跌）。
一套策略的真实水平不看它最好的一段，看它最差的一段亏多少、回撤多深、
连亏多少笔 —— 因为实盘你一定会撞上那一段。

---

## 三、参数优化：两阶段，且只动 2–3 个

单参数扫描很容易把结果拟合到历史噪音上。约束自己：

**一次只优化 2–3 个参数**，而且必须是有明确疑问的那几个，不是"全选一遍"。

### 优先级最高的一组：触发周期

`InpEntryTF=M1` 是为了解决"进场太慢"——把一个形态的确认延迟从 5 分钟压到 1 分钟。
但快和准是一对矛盾：M1 触发根噪音大得多，信号数量涨上去的同时，
假信号也涨上去了。这一刀到底划算不划算，实盘要跑好几周才看得出来，
回测几分钟就能给出答案。

| 参数 | 现值 | 扫描范围 | 在问什么 |
|---|---|---|---|
| `InpEntryTF` | M1 | M1 / M5 / M15 | 快多少才开始亏钱 |
| `InpAdxMin` | 8.0 | 6 → 20，步长 2 | 放宽到 8 是拍的，从没验证过 |

看结果时**别只看总成交数**。要对比的是同一段行情下 M1 与 M5 的
**每笔期望值（净利 ÷ 笔数）**：如果 M1 笔数翻了 5 倍而每笔期望值掉了 5 倍以上，
那就是纯粹在多交手续费，快没有换来任何东西。

### 其余有疑问的三个

| 参数 | 现值 | 疑问从哪来 | 扫描范围 |
|---|---|---|---|
| `InpFadeExitMinR` | 0.45 | 8/21 出现一串 0.21 / 0.22 / 0.45 / 0.47 / 0.59R 的小赢单，怀疑动量衰竭离场在**系统性砍掉赢单** | 0.30 → 1.20，步长 0.15（含"关掉"档） |
| `InpScoreMinB` | 4 | 质量闸门。调高 = 单少质高，调低 = 单多质糙。之前为了"能开出单"一路降到 4，从没验证过这是不是最优 | 3 → 7，步长 1 |
| `InpLevelExitMinR` | 1.20 | 上一轮修 bug 时拍的数（原来 0.70 把每个赢单都卡死在 0.70R），修对了方向，但 1.20 这个值本身没测过 | 0.80 → 2.00，步长 0.20 |

### 两阶段跑法

1. **初筛**：用 `1 分钟 OHLC` + 遗传算法，把上面的组合扫一遍。快，用来砍掉明显差的区域。
2. **验证**：把前 5–10 组用 **真实 tick** 重跑一遍。
   初筛里排第一的那组，在真实 tick 下经常掉到中游 —— 掉得越狠，说明它越依赖
   K 线内部的路径细节，也就越不可靠。

### 选参数看什么

优化结果**别按"余额最大"排序挑第一名**。那一名几乎总是过拟合。看两件事：

- **参数曲面平不平**：把结果按参数值排开，你要的是一个**连续的高原**，
  不是一根孤立的尖峰。0.60 好、0.75 好、0.90 也好 → 选 0.75，稳；
  0.75 极好而 0.60 和 0.90 都亏 → 这是噪音，别碰。
- **利润因子落在哪**：1.5–2.5 是合理区间。低于 1.3 说明扣掉成本后基本白干；
  **高于 3.0 通常是过度优化的信号**，不是好消息。
  （对照：我们实盘 31 笔算出来 PF = 10.75 —— 这个数字本身就说明样本不能信。）

### 样本外验证（必做）

测试器设置页有 **前进（Forward）** 选项：选 `1/4`，MT5 会自动用前 3/4 的数据
优化、再把最优参数拿到最后 1/4（优化器**没见过**的数据）上跑一遍。

判断标准很简单：**样本外的利润因子应该在样本内的 60% 以上。**
样本内 PF 2.5、样本外 0.9 —— 这组参数是拟合出来的，扔掉，别心疼。

---

## 四、回测测不到的三件事

看结果时必须一直记着这三条，否则会把回测数字当承诺：

1. **新闻过滤在回测里完全没生效。**
   MT5 测试器里经济日历不可用（代码里 `NewsInWindow()` 直接 return false），
   而 `InpManualNewsTimes` 是空的。所以回测跑的是"从不避新闻"的版本，
   实盘会比回测**少开一些单**，也会少掉几次数据跳空的爆亏。
2. **点差被低估。**
   即使真实 tick 模式带了历史点差，黄金在数据发布前后点差会瞬间放大数倍，
   而那几秒的 tick 往往正是缺失最多的。
3. **滑点。**
   测试器默认理想成交。真实的止损滑点只会让结果更差，不会更好。

**所以：回测结果是乐观上界。** 一套在回测里 PF 1.6 的策略，实盘打个七折是常态；
一套回测 PF 1.05 的策略，实盘就是亏的。

还有一条不属于"测不到"但同样重要：**换券商就要重测**。
点差、执行、历史数据质量都是券商特有的，别人发的漂亮回测图不适用于你的账户。

---

## 五、和实盘数据对起来看

回测跑完，用同一套口径和实盘对照 —— 这才是这份工作的落点：

```powershell
# 单次回测想留 CSV：把 InpTesterQuiet 设为 false 再跑
# 日志在  <数据目录>\Tester\Agent-*\MQL5\Files\XAUUSD_ScalperGuard_log.csv
python analyze_scalperguard.py --log XAUUSD_ScalperGuard_log.csv
```

重点对三组数字：

| 看什么 | 实盘（31 笔） | 回测应该给出什么 |
|---|---|---|
| 多空比例 | 100% BUY | 接近 50/50；差太多就是不对称 |
| 盈亏平衡胜率 | 55%（剔除极值后） | 回测的实际胜率必须**明显高于**这个数才有余量 |
| 各入场路径的贡献 | A/B 路径占几乎全部 | C/D 若仍接近 0，就该删 |
| 最大连亏 / 最大回撤 | 未见（样本太顺） | 这是回测最有价值的产出 —— 提前知道最坏的一段长什么样 |

---

## 六、一句话版本

> 先跑一次 1 年以上、真实 tick、`ScalperGuard_backtest.set` 的回测；
> 第一眼**不看净利，看有没有做空单**；
> 然后只优化 `InpFadeExitMinR` / `InpScoreMinB` / `InpLevelExitMinR` 这三个，
> 用 Forward = 1/4 做样本外验证；
> 选**高原**不选尖峰，PF 超过 3.0 当作警报而不是喜讯。

---

### 参考

- [Mastering MT5 Strategy Tester: Backtest, Optimize, and Validate Strategies](https://www.mql5.com/en/blogs/post/766653)
- [Not All 99% Backtests Are Equal: How Tick Data Quality Impacts Your Strategy in MetaTrader 5](https://www.mql5.com/en/blogs/post/762517)
- [MT5 Backtest Modeling & History Quality Explained: Real Ticks, Costs, and What to Trust](https://forexrobotlab.com/mt5-backtest-modeling-history-quality/)
- [Optimisation robustness and avoiding overfitting (MQL5 forum)](https://www.mql5.com/en/forum/459197)
- [MetaTrader 5 Strategy Tester: Complete Backtesting Tutorial](https://backtrex.com/en/blog/metatrader-5-strategy-tester-backtesting-tutorial)
