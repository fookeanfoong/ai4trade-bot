__pycache__/
*.pyc
.env
KILL_SWITCH.txt
moomoo_trader_state.json
moomoo.log

# MT5 监测器的本地运行产物(在你自己电脑上生成,不入库)
gold_journal.json
reports/gold_health.md
reports/gold_alert.txt
reports/monitor_task.log
preset.json

# Binance local book — runs on YOUR PC, so these stay local (and the key file
# must NEVER be committed).
binance_keys.bat
binance_baseline.json
live_trader_binance_state.json
reports/live_trader_binance/

# Node / web build artifacts — installed fresh per session, never committed
node_modules/
.next/
*.log
