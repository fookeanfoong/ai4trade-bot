# 外汇模块(EUR/USD · 信号引擎 + 手动执行)

给 **$200 本金 / 马来西亚 / MT5 手动执行** 的外汇信号系统。和股票、加密两本账
完全隔离:独立的数据源、独立的信号文件、独立的 workflow,不碰 `live_trader.py`
的任何状态。

**它出信号,你点单。** 机器负责它擅长的(不知疲倦地盯盘、算仓位、拒绝不合格的
setup),你负责它做不到的(在 MT5 上点那一下)。

---

## 为什么是手动执行,不是全自动

不是能力问题,是**你所在地区拿不到 API**。

OANDA 开发者文档([API Comparison](https://developer.oanda.com/rest-live-v20/api-comparison/))
原话:v20 REST API 对所有分支开放,**除了 OANDA Global Markets 和 OANDA TMS
BROKERS S.A.**。而马来西亚开户会被分到 **OANDA Global Markets Ltd(BVI 注册)**
—— 正好是被排除的那个。

所以:

| | 账户号长相 | v20 API |
|---|---|---|
| fxTrade / fxTrade Practice | `101-011-1234567-001` | ✅ |
| **MT5(`OANDA_Global-Demo`,你的)** | `1715551949` 纯数字 | ❌ **结构性不可用** |

再注册多少次都是 MT5,这不是操作失误。而 MT5 没有 REST API,唯一的驱动方式是
`MetaTrader5` Python 包 —— 它**必须**有一台 Windows 机器常开着跑 MT5 桌面终端,
GitHub Actions 是无头 Linux,跑不了。

**结论:行情和券商解耦。** 信号引擎用 Yahoo 免费行情自己算,你用哪家券商跟它
无关。哪天你换到有 REST API 的券商,执行层(`live_trader_forex.py`)已经写好在
那里了,改一个开关就能接上。

---

## 为什么是 H1,不是 H4

这条也是被数学逼出来的,不是偏好。

MT5 最小手数 **0.01 手 = $0.10/点**。$200 本金:

| 周期 | 典型止损 | 0.01 手实亏 | 占本金 | 能做吗 |
|---|---|---|---|---|
| H4 | 40~50 点 | $4.00~5.00 | 2.0~2.5% | ❌ 超 2% 硬上限,**每笔都会被拦** |
| **H1** | **15~20 点** | **$1.50~2.00** | **0.75~1.0%** | ✅ |

**在 MT5 上,风险大小完全由止损距离决定** —— 因为手数已经卡死在最小值,你没有
调节余地。20 点止损 = 1% 风险,40 点 = 2%(上限),超过 40 点就不该做。

信号引擎会自己算这件事:算出来不足 0.01 手时,它会告诉你用 0.01 手的实际风险
是多少;超过 2% 硬上限的,直接标 ⛔ **这笔在 MT5 上不该做**。

---

## 部署(不需要任何账户、任何 Secret)

数据源是 Yahoo 免费行情,**不需要 token,不需要开户**。

👉 https://github.com/fookeanfoong/ai4trade-bot/actions/workflows/forex_signals.yml

点 **Run workflow** 就能跑。跑完看仓库里的:

- **`signals_forex.md`** ← 给你看的,直接照着点单
- `signals_forex.json` ← 给程序看的

排程:每小时第 5 分钟(H1 收盘后)自动跑一次。

本地跑:

```bash
python3 fx_data.py EUR_USD          # 只测数据源
python3 generate_signals_forex.py   # 生成信号
```

---

## 怎么读信号卡

```
### 方案B · 逆势做空(双顶拒绝) — `triggered`
- 入场 1.15617 · 止损 1.15767(15.0 pips) · TP1 1.15317(1:2.0)
- 仓位:MT5 0.01 手 · 风险 $1.5
- 确认:✅阻力区拒绝K线 / ✅RSI顶背离(必需) / ✅MACD柱缩短 / ✅触及>=2次
- 否决:H4 收盘站上 1.15687 = 认错离场
```

状态只有这几种,**只有 `triggered` 才动手**:

| 状态 | 意思 |
|---|---|
| `triggered` | ✅ 触发 + 确认都够了,可以下单 |
| `armed` | 条件单待触发 —— 挂着,别手动追 |
| `blocked_no_divergence` | 逆势方案没有 RSI 背离 → 不做 |
| `blocked_weak_confirmation` | 触发了但确认不够 → 不做 |
| `blocked_event` | 高影响数据前 12 小时 → 不开新仓 |
| `wait_market_closed` | 周末休市 |

在 MT5 上下单时:**止损和止盈必须跟单子一起设**(新订单窗口里的 S/L 和 T/P 栏),
不要"先进场回头再补" —— 补之前那段时间你就是个裸单。

**出场策略:TP1 全平。** 到 TP1 整仓走人,固定 1:2,不留尾仓、不移保本、不追 TP2。
这是你选的"吃小赢面广",并且因为 S/L 和 T/P 都挂在券商那里,**你人不在电脑前也
会自动成交**。

---

## 护栏

| 护栏 | 默认 | 为什么 |
|---|---|---|
| 风险上限 | 1%,**硬顶 2%** | 调到 5% 会被代码夹回 2% |
| 止损下限 | max(15 pips, 8×点差) | 纯按 ATR 缩放会算出 10 点止损,点差就占 20%,必被噪音扫掉 |
| MT5 手数校验 | 0.01 手风险 > 2% 就标 ⛔ | 手数卡死在最小值时,风险由止损距离单独决定 |
| 事件封锁 | 高影响事件前 12h | CPI/非农前的突破是低质量突破,方向对了也可能被数据抹掉 |
| 未收盘 K 线 | 一律丢弃 | 用未收盘的 close 算信号 = 未来函数 |

> ⚠️ `forex_events.json` 的时间来自公开周报,且**原文星期几与真实日历对不上**
> (2026-08-12 是周三,原文写周二)。用之前请到 ForexFactory 核对,把
> `verify: true` 改掉。

> ⚠️ **H4/H1 对齐差异**:本模块按 **UTC** 分桶,你的 MT5 按**券商服务器时间**
> 对齐。两边 K 线会错位,阻力位可能差几个点。要对齐改 `FOREX_H4_ANCHOR_UTC`。

---

## 什么时候才谈自动交易

先用手动跑满 **20 笔**,记录每一笔:信号说什么、你做了什么、结果如何。
然后回答一个问题:**这 20 笔里,亏损是因为信号错,还是因为你没按信号做?**

- 如果是信号错 → 自动化只会让你亏得更快,先改规则
- 如果是你没按信号做 → 那才是自动化真正能解决的问题

**不要设周收益目标。** 有目标就会重仓,重仓就会归零。README 里那句
「$400/week on $100 ≈ 400% 周收益率 — 不可能实现」对外汇同样成立。

---

*研究/学习用途,不构成投资建议或收益承诺。外汇保证金交易杠杆高、风险大,可能损失全部本金。*
