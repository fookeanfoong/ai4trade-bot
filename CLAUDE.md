# 与用户的工作约定（XAUUSD ScalperGuard）

## 收到报告 / 日志时的默认流程

用户发来 `ReportHistory*.xlsx`、`XAUUSD_ScalperGuard_log.csv` 或 MT5 journal 时：

1. **直接分析 + 直接改 + 直接推**，不要先问要不要改、改哪个。
2. 改完再说：结论是什么、改了哪几个参数、代价是什么。**报告在前，长篇论证不要**。
3. 用户已多次表示不需要反复确认，也不需要每次重复同样的风险提示。
   同一条警告说过一次就够了（例如追高的代价、盈亏比与保本胜率的关系）。

唯一例外（这两条要问）：
- 把 `InpAllowLiveAccount` 改成 true（真实账户）
- 删除或重写已有的历史数据 / 日志

## 分析口径（踩过的坑，别再踩）

- **报告 xlsx 里的 `S / L` 是平仓时的止损**，已被保本/追踪移动过，**不是开仓止损**。
  用它算 R 会严重失真（会算出 +12R 这种数）。用金价距离和美元，或用 EA 自己的
  CSV 日志里 `[OPEN]` 记的初始止损。
- 报告不记录**是哪道出场触发的**。要定位「赢单为什么被砍早」必须要 CSV 日志，
  看 `[EXIT] / [TP1] / [TRAIL] / [GIVEBACK] / [MANAGE] / [STALL]` 标签。
- 评估表现看 **R 倍数**，不看百分比 —— $200 账户上 64% 的单子是最小手 0.001，
  那些单的实际风险由最小手决定，不由 `InpRiskPct` 决定。

## 截至 2026-08-25 的结论

- 累计 133 笔。净 +$1819，但**几乎全部来自 08-14 一天**（+$1889，$10k 仓位）。
- 换到 $200 虚拟本金后 114 笔，净 **−$4.47**。
- 两个阶段**入场出场行为几乎相同**：赢单金价均走 +$7.5、亏单 −$11.3。
  盈亏比 0.67 → **保本胜率需 60%**，实际 60.5%。正好卡在平衡线上。
- 08-25 连亏日：赢单只走 +$3.88（0.4×ATR），亏单走 −$11.23（1.2×ATR），
  比值 0.35 → 需 74% 胜率。**出场不对称且方向反了：赢单砍太早，亏单放到满止损。**
  这是当前最该修的地方。
- 做空已经能发生（08-24 傍晚 18 笔，全赢但合计仅 +$11，全是最小手）。

## 硬性红线（任何配置都不许破）

- 每一笔都必须带止损；止损只能往盈利方向移动
- 不加仓摊平、不马丁
- `InpAllowLiveAccount=false` 除非用户明确要求改
- 八个 `.set` 预设的**键集必须完全一致**（改完用脚本核对），否则切换预设会留残值

## 常用命令

```bash
# 预设键集一致性检查
cd mql5 && ref=$(grep -o '^Inp[A-Za-z]*' ScalperGuard_aggressive.set|sort|md5sum)
for f in ScalperGuard_*.set; do h=$(grep -o '^Inp[A-Za-z]*' $f|sort|md5sum); \
  [ "$h" = "$ref" ] || echo "$f MISMATCH"; done

# .set 必须是 CRLF
python3 -c "d=open(P,'rb').read().replace(b'\r',b'').replace(b'\n',b'\r\n');open(P,'wb').write(d)"
```

改 `.set` 不用重编译；改 `.mq5` 必须让用户关掉 MetaEditor 标签页 → 重跑
`update_mt5.ps1` → F7 → 删 EA 重挂。
