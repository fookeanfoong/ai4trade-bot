# MT5 ↔ Claude 桥接设计

目标：让 Claude 能读到 EA 的运行数据、并在**人确认后**把参数改动推回 MT5，
不需要你手动导日志、手动改对话框。

---

## 一、先划清能力边界

| 你可能期待的 | 实际 | 原因 |
|---|---|---|
| Claude 一直盯盘 | ❌ | 我不是常驻进程。对话之间我不存在 |
| 秒级 / 每根 M5 响应 | ❌ | 定时唤醒下限是**每小时**一次 |
| Claude 实时改策略 | ❌ 也不该做 | 见下节 |
| Claude 定期读数据、给出改动建议 | ✅ | 每小时 / 每天 |
| 参数改动免手动、推回 MT5 | ✅ | 控制通道，带硬钳位 |
| 配置错、点差异常时自动停手 | ✅ | 但由 **EA** 自己判断，不等我 |

### 为什么"实时调策略"是反模式

按小时盯着最近几笔盈亏调参数，不是优化，是**过拟合 + 报复性交易**，
只不过执行者从人换成了 AI —— 而且更难察觉，因为它看起来有理有据。

EA 源码里那句注释同样适用于我：

> 连亏之后最危险的不是下一笔的期望值变差（它没变），是人开始加码找回来。
> 让规则替你停手。

十几笔样本里看到的"规律"绝大多数是噪音。**参数改动的门槛应该是"几十笔以上
的统计差异"，不是"我刚看到连亏三笔"。**

### 正确的分工

| 层 | 执行者 | 节奏 | 依据 |
|---|---|---|---|
| 交易决策 | EA（规则） | 每根 M5 | 写死的逻辑，确定性、可回测 |
| 熔断停手 | EA 自己 | 实时 | 日内限额、连亏 —— 不需要等我 |
| 参数调整 | Claude 提议 + 人确认 | 每天 / 每周 | ≥30 笔的统计 |
| 结构性改动 | Claude 改代码 + 重编译 | 按需 | 日志暴露出的设计缺陷 |

---

## 二、架构

```
┌────────────────┐   ①遥测     ┌─────────────┐   ③读取    ┌──────────┐
│  MT5 + EA      │ ──────────► │   传输层     │ ◄───────── │  Claude  │
│                │             │             │            │          │
│ status.json    │             │  GitHub     │  ④写入配置 │ 每小时/  │
│ log.csv        │ ◄────────── │  仓库/Gist   │ ──────────►│ 每天唤醒 │
└────────────────┘   ②控制     └─────────────┘            └──────────┘
      ▲                                                         │
      │                          ⑤ 建议 + 人确认                 │
      └──────────────────────  你 ◄─────────────────────────────┘
```

**已经做好的：**
- `PublishStatus()` → `MQL5\Files\XAUUSD_ScalperGuard_status.json`（每 30 秒）
- `LogLine()` → `MQL5\Files\XAUUSD_ScalperGuard_log.csv`（每个决策）
- `ReportReasons()` → 拒绝原因分布（每小时）
- `ReportSpreadHour()` → 点差画像（每小时）
- `Push()` → 手机推送（关键事件）

**还缺的：** ① 遥测上行、② 控制下行、③④ 定时唤醒。

---

## 三、控制通道的安全边界（最重要的一节）

远程能改参数 = 远程能操纵你的账户。所以**钳位必须写死在 EA 里**，
而不是靠"我不会发坏指令"。

### 绝对不可远程修改（改了直接忽略并告警）

- `InpAllowLiveAccount` —— 永远不能远程从 Demo 切到实盘
- 任何导致**无止损持仓**的行为
- `InpRiskPctMax` 的 2% 天花板 —— 远程值一律 `MathMin(远程值, 2.0)`
- 止损方向 —— 只能往盈利方向移，这条在 `ManagePositions` 里，不接受任何覆盖

### 可远程调整，但硬钳位

| 参数 | 允许区间 | 钳位方式 |
|---|---|---|
| `InpRiskPctDefault` | 0.1 ~ 2.0 | `MathMin(v, InpRiskPctMax)` |
| `InpMaxSpreadUSD` | 0.10 ~ 1.00 | 超出取边界 |
| `InpMinScore` | 4 ~ 7 | 超出取边界 |
| `InpAdxMin` | 5 ~ 40 | 超出取边界 |
| `InpMinRR` | 0.8 ~ 3.0 | 超出取边界 |
| 时段 / 波动率区间 / 触发灵敏度 | 各自合理区间 | 同上 |
| **紧急开关** `halt` | true/false | true = 停止开新仓（已有持仓照常按规则管理）|

### 命令文件格式

```json
{
  "issued_at": "2026-08-13T09:00:00Z",
  "expires_at": "2026-08-14T09:00:00Z",
  "reason": "亚洲盘点差中位数 0.62，超过上限的时间占 71%",
  "halt": false,
  "params": { "InpSessionStartHour": 10, "InpSessionEndHour": 23 }
}
```

- **`expires_at` 是必需的。** 过期即整份失效，回退到 `.set` 的值。
  防止一条临时指令因为我没醒过来而永久生效。
- 每次应用都写一行 `[CONTROL]` 日志，记下**改了什么、从什么改到什么、为什么**。
- 钳位触发时写 `[CONTROL-CLAMP]`，记录被拒的原始值。

---

## 四、分阶段

### 阶段 1：控制下行（无需任何外部服务）

EA 用 `WebRequest` 定期 GET 一个 **public raw URL**（本仓库的
`control/commands.json`），解析、钳位、应用。

- 不需要 token —— 公开仓库的 raw 是匿名可读的
- 不需要服务器
- 需要：MT5 → 工具 → 选项 → 智能交易系统 → 把
  `https://raw.githubusercontent.com` 加进 **允许的 WebRequest URL 列表**

我改配置 = 往仓库提交一个 JSON。你不用碰 MT5 对话框。

### 阶段 2：遥测上行

三个选项，复杂度递增：

| 方案 | 优点 | 代价 |
|---|---|---|
| **手动**：你把 `log.csv` 发我 | 零基础设施，现在就能用 | 需要你动手 |
| **Gist**：EA POST 到一个 secret Gist | 自动、无服务器、不污染仓库提交历史 | EA 里要放一个 GitHub token |
| **Vercel 接口**：EA POST 到自建端点 | token 是自签的、可随时吊销；能接看板 | 要维护一个服务 |

> ⚠️ token 放进 EA 输入框 = 存在图表模板和 `.set` 文件里。
> 用**细粒度、只对一个 gist 有写权限**的 token，别用全权限的。

### 阶段 3：定时唤醒

建一个 Routine（定时任务），每天收盘后触发我：读遥测 → 算分组期望值 →
写一份报告 + 提议的参数改动 → **等你确认**再落地成 `commands.json`。

**默认不自动应用。** 只有一类例外可以预先授权：
配置性错误和成本异常（例：点差中位数跳到上限的 2 倍 = 经纪商侧变了）
→ 允许自动 `halt: true` 停手，因为"停手"不会亏钱。

---

## 五、这套东西不解决什么

- 不会让负期望的策略变正。参数是围着 edge 转的，edge 不在参数里。
- 不会替代样本量。30 笔以下的任何"结论"都是噪音，工具再顺手也一样。
- 不会消除点差成本。$0.40 点差配 $3 止损就是每笔先付 13%，
  这笔账只能靠换品种 / 换时段 / 换经纪商解决，不能靠调参数。

---

*Demo 阶段的工具。真实账户前请自己复核每一条钳位。*
